# Count-Project

# 📝 Real-Time Character Counter

A simple and modern web app that lets users type text into a textarea while tracking the number of characters in real time.  
It displays how many characters have been typed, how many are remaining, and visually indicates progress using a dynamic progress bar.

---

## 🚀 Features

- Real-time character counter that updates as you type  
- Displays current and remaining characters (e.g., `150 / 200 characters`)  
- Prevents typing beyond the character limit  
- Visual progress bar that fills as you type  
- Color changes as you approach the limit:  
  - 🟦 Blue → Safe range  
  - 🟧 Orange → Near limit (80%)  
  - 🔴 Red → Limit reached  
- Smooth animations and modern UI styling  

---

## 🧠 Technologies Used

- **HTML5** – Structure and markup  
- **CSS3** – Styling and progress bar design  
- **JavaScript (ES6)** – Real-time updates and logic handling  

---
   ```bash
   git clone https://github.com/<your-username>/character-counter.git
